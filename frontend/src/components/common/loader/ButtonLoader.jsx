
const ButtonLoader = () => {
  return (
    <div>ButtonLoader</div>
  )
}

export default ButtonLoader



// PageLoader → Full-screen authentication check.
// SectionLoader → Product table loading while keeping the layout visible.
// ButtonLoader → Spinner inside "Login", "Save", or "Submit" buttons.