export const DEFAULT_CART_QUANTITY = 1;

export const MAX_CART_ITEM_QUANTITY = 100;

export const MAX_CART_DISTINCT_ITEMS = 50;

/**
 * Possible results of an Add to Cart operation.
 */
export const CART_ACTIONS = Object.freeze({
  ADDED: "added",
  UPDATED: "updated",
});


